/*Write a function called find missing number that takes a list of integers ,numbers  
where each number in the list is an integer between 1 and 1000 inclusive and 
some of those numbers may be missing the function should return as a list of all missing numbers range 1 to 1000
    1.list is not sorted 
    2.list contains duplicates which must ignored 
    3.function must return missing numbers in serially*/
