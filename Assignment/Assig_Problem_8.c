#include <stdio.h>
int main()
{
    int a;
    scanf("%d", &a);
    float in[a]; // Declaring array size
    // set loop for storing arrays
    
    scanf("%f", &in[a-1]);
    printf("%f", in[a-1]);
    return 0;
}
